// const user = {
//     name: "Mike",
//     age: 30,
// };

// Object.assign(복사할 객체, 복사할 내용이 담긴 객체)
// const user2 = Object.assign({}, user);
// user2.name = "Tom";
// console.log(user);
// console.log(user2);

// Object.keys(객체) : 객체의 key값들을 배열로 반환
// const result = Object.keys(user);
// console.log(result);

// Object.values(객체) : 객체의 value값들을 배열로 반환
// const result = Object.values(user);
// console.log(result);

// Object.entries(객체) : 객체의 key, value값들을 배열로 반환
// const result = Object.entries(user);
// console.log(result);

// Object.fromEntries(배열) : 배열을 객체로 변환
// const arr = [
//     ['mon', '월'],
//     ['tue', '화'],
// ];
// const result = Object.fromEntries(arr);
// console.log(result);

